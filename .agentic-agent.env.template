# Template: .agentic-agent.env

# Copy this file to your home directory as ~/.agentic-agent.env
# Replace placeholders with your actual keys
# chmod 600 ~/.agentic-agent.env (to secure the file)
# source ~/.agentic-agent.env (to load environment variables)

# ═══════════════════════════════════════════════════════════════════
# === BLOCKCHAIN & RPC CONFIGURATION ===
# ═══════════════════════════════════════════════════════════════════

# Solana RPC Endpoints
SOLANA_RPC_URL="https://api.devnet.solana.com"
# Production: https://api.mainnet-beta.solana.com
# Testnet: https://api.testnet.solana.com

# Neon EVM RPC (Solana EVM environment)
NEON_RPC_URL="https://devnet.neonevm.org"
# Production: https://neon-proxy-mainnet.solana.com

# ═══════════════════════════════════════════════════════════════════
# === API KEYS (CRITICAL - NEVER SHARE) ===
# ═══════════════════════════════════════════════════════════════════

# Helius - Solana RPC & WebSocket Provider
# Get from: https://dashboard.helius.dev
# 1. Sign up
# 2. Create a project
# 3. Copy the API Key
HELIUS_API_KEY="your_helius_api_key_here"

# Alchemy - Multi-chain RPC Provider
# Get from: https://alchemy.com
# 1. Sign up
# 2. Create an Application
# 3. Copy the API Key
ALCHEMY_API_KEY="your_alchemy_api_key_here"

# Biconomy - Gasless Transaction Relay
# Get from: https://dashboard.biconomy.io
# 1. Sign up
# 2. Create a Project
# 3. Copy Dashboard API Key
BICONOMY_API_KEY="your_biconomy_api_key_here"

# ═══════════════════════════════════════════════════════════════════
# === WALLET & PRIVATE KEYS ===
# ═══════════════════════════════════════════════════════════════════

# Ethereum/EVM Private Key
# ⚠️  DANGER: This grants full access to your account
# NEVER use mainnet keys in development!
# Get from MetaMask: Account > Export Private Key
# Should START with 0x followed by 64 hex characters
PRIVATE_KEY="0xYOUR_ETH_PRIVATE_KEY_HERE"

# Solana Treasury / Wallet Address (Public Key)
# Get from Phantom, Solflare, or Ledger
# Format: Base58 string (44-44 characters) starting with 'S'
SOLANA_TREASURY="YourSolanaPublicKeyHere"

# ═══════════════════════════════════════════════════════════════════
# === OLLAMA CONFIGURATION (LOCAL LLM) ===
# ═══════════════════════════════════════════════════════════════════

# Ollama API URL
# Local (Android/Termux): http://localhost:11434
# Remote (if running on another machine): http://192.168.1.100:11434
OLLAMA_API_URL="http://localhost:11434"

# Ollama Model to Use
# Options: neural-chat, orca-mini, mistral, llama2, falcon
# Recommended for Android: neural-chat (1.6GB, fast, good quality)
# Lightweight: orca-mini (1.3GB, very fast)
# Quality: mistral (4.1GB, slower, better responses)
OLLAMA_MODEL="neural-chat"

# Ollama Timeout (seconds)
# Increase if on slower devices or networks
OLLAMA_TIMEOUT="30"

# ═══════════════════════════════════════════════════════════════════
# === AGENT CONFIGURATION ===
# ═══════════════════════════════════════════════════════════════════

# Agent Loop Interval (milliseconds)
# 30000 = 30 seconds (default)
# 60000 = 1 minute
# Increase for slower devices or to reduce API calls
AGENT_LOOP_INTERVAL="30000"

# Random variance for cycle timing (milliseconds)
# Prevents thundering herd if running multiple agents
AGENT_LOOP_VARIANCE="5000"

# Log Level: debug, info, warn, error
# Use 'error' to reduce logging and save storage
LOG_LEVEL="info"

# ═══════════════════════════════════════════════════════════════════
# === DEPLOYMENT SETTINGS ===
# ═══════════════════════════════════════════════════════════════════

# Network for contract deployment
# Options: neon-devnet, neon-mainnet, solana-devnet, solana-mainnet
DEPLOY_NETWORK="neon-devnet"

# Gas settings for EVM deployment
# Gas limit (in wei)
DEPLOY_GAS_LIMIT="200000"

# Gas multiplier (1.0 = normal, 1.5 = 50% more gas price)
# Increase if transactions keep failing
DEPLOY_GAS_MULTIPLIER="1.0"

# Initial account balance for gas (dry run)
# Used for simulation testing
DEPLOY_TEST_BALANCE="1000000000000000000"

# ═══════════════════════════════════════════════════════════════════
# === OPTIONAL: ADVANCED FEATURES ===
# ═══════════════════════════════════════════════════════════════════

# Enable auto-update of agent code (be careful!)
# true or false
AGENT_AUTO_UPDATE="false"

# GitHub token for automated PRs (if auto-updating)
# Get from: https://github.com/settings/tokens
GITHUB_TOKEN=""

# Enable debug output (lots of logs)
# true or false
DEBUG_MODE="false"

# Custom webhook for notifications (Discord/Slack)
# Leave empty to disable
WEBHOOK_URL=""

# ═══════════════════════════════════════════════════════════════════
# === SECURITY REMINDERS ===
# ═══════════════════════════════════════════════════════════════════

# ⚠️  NEVER:
# - Commit this file to git
# - Use real mainnet private keys
# - Share your API keys
# - Leave this file world-readable (chmod 600)
# - Push to public repositories
#
# ✅ ALWAYS:
# - Use testnet keys first
# - Rotate API keys periodically
# - Keep backups in secure location
# - Review transactions before signing
# - Use unique keys per environment (dev/test/prod)

# ═══════════════════════════════════════════════════════════════════
# === GETTING API KEYS ===
# ═══════════════════════════════════════════════════════════════════

# HELIUS (Solana RPC):
#   1. Visit https://dashboard.helius.dev
#   2. Click "Sign Up"
#   3. Create account
#   4. Create a new project
#   5. Copy API Key from dashboard

# ALCHEMY (EVM RPC):
#   1. Visit https://alchemy.com
#   2. Sign up with email or Google
#   3. Create an application
#   4. Select network (Ethereum, Polygon, etc.)
#   5. Copy API Key

# BICONOMY (Gasless):
#   1. Visit https://dashboard.biconomy.io
#   2. Sign up
#   3. Create project
#   4. Copy Dashboard API Key

# PRIVATE KEY / WALLET:
#   MetaMask:
#     Account menu > Settings > Security & Privacy > Reveal Secret Recovery Phrase
#     OR: Account menu > Export Private Key
#   
#   Phantom (Solana):
#     Settings > Wallet > Export Private Key
#   
#   Ledger:
#     Use Ledger Live or hardware-backed wallet

# ═══════════════════════════════════════════════════════════════════
# === SETUP INSTRUCTIONS ===
# ═══════════════════════════════════════════════════════════════════

# 1. Copy this template:
#    cp .agentic-agent.env.template ~/.agentic-agent.env

# 2. Edit with your keys:
#    nano ~/.agentic-agent.env

# 3. Secure the file:
#    chmod 600 ~/.agentic-agent.env

# 4. Verify permissions:
#    ls -la ~/.agentic-agent.env
#    Should show: -rw------- (only your user can read)

# 5. Load in your shell:
#    source ~/.agentic-agent.env

# 6. Verify keys are loaded:
#    echo "Helius: $HELIUS_API_KEY"
#    (Should show your key, not empty)

# ═══════════════════════════════════════════════════════════════════
# For more information, see:
# - TERMUX_ANDROID_SETUP.md (complete Android guide)
# - QUICK_REFERENCE.md (quick commands)
# - README.md (overview)
# ═══════════════════════════════════════════════════════════════════
